name: CI

on:
  push:
    branches:
      - master
  pull_request:
    branches:
      - master

jobs:
  dialyze:
    runs-on: ubuntu-latest
    container:
      image: erlang:24.1
    steps:
      - uses: actions/checkout@v2
      - run: make dialyze
